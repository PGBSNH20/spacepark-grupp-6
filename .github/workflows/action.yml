name: Kevin och Mikael
on: push
jobs: 
  build: 
    name: Spacepark build
    
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkhout@v2
    - uses: actions/setup-dotnet@v1
      with: 
        dotnet-version: 5.0.x
    - name: Spacepark 
      run: dotnet build SpacePark/SpacePark.csproj
        
    
  
